apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Since they are in the same docker network, we use the container name
    url: http://prometheus:9090
    isDefault: true
    editable: false